<div class="content">
  <p id="notice"><%= notice %></p>

  <h5 align=center>Lista de  Clientes</h5>
  <%= link_to "Nuevo cliente", new_client_path(), class: "btn btn-success-waves indigo" %>
  <table>
    <thead>
      <tr>
        <th>Nombre</th>
        <th>Apellido</th>
        <th>Fecha de pago</th>
        <th>Suscripción</th>
        <th>Celular</th>
        <th>Correo</th>
        <th>Activo</th>


        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @client.each do |client| %>
        <tr>
          <td><%= client.nombre %></td>
          <td><%= client.apellido %></td>
          <td><%= client.fecha_ultpago %></td>
          <td><%= @nombre %></td>
          <td><%= client.cel %></td>
          <td><%= client.correo %></td>
          <td><%= client.activo %></td>
          <td><%= link_to 'Info', edit_client_path(client) %></td>
          <td><%= link_to 'Eliminar', client, method: :delete, data: { confirm: 'Are you sure?' } %></td>




        </tr>
      <% end %>
    </tbody>
  </table>

</div>
